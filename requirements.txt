psycopg2-binary>=2.9
kaggle>=1.6

# Plot deps — pinned to versions whose cp311 wheels include
# manylinux2014_x86_64 (glibc 2.17), so they install without a C
# compiler on the Innopolis cluster (RHEL 7, GCC 4.8.5). Newer pandas /
# numpy require glibc 2.28 wheels or compile from source.
numpy>=1.24,<2
pandas>=2.0,<2.2
matplotlib>=3.7,<3.9
# Matplotlib transitive deps — pinned because newer releases dropped
# manylinux2014 wheels (require glibc 2.28; cluster has 2.17) and would
# compile from source, which needs GCC>=9 / C++17 (cluster has 4.8.5).
contourpy>=1.1,<1.3
pillow>=10,<11
kiwisolver>=1.4,<1.5
